// import React from 'react';

const LeftSidebar = () => (
    <aside className="w-64 p-8 bg-black border-r border-r-gray-400 min-h-screen text-white">
      {/* <nav>
        <ul className="space-y-2">
          {["Feed", "Chats", "Videos", "Groups", "Bookmarks", "Questions", "Jobs", "Events"].map((item) => (
            <li key={item} className="flex items-center space-x-2">
              <span className="w-6 h-6 bg-gray-300 rounded-full"></span>
              <span>{item}</span>
            </li>
          ))}
        </ul>
      </nav>
      <button className="mt-4 w-full bg-blue-500 text-white p-2 rounded">Show More</button>
      <div className="mt-6">
        <h3 className="font-bold mb-2">Close Friends</h3>
        <ul className="space-y-2">
          {["Willfried Wiechmann", "Zahariy Bielinskiy", "Falko Leuschner", "Malusha Strashkevich"].map((friend) => (
            <li key={friend} className="flex items-center space-x-2">
              {/* <Image src="/placeholder.svg?height=32&width=32" alt={friend} width={32} height={32} className="rounded-full" /> */}
              {/* <span>{friend}</span>
            </li>
          ))}
        </ul>
      </div> */} 
    </aside>
  )

export default LeftSidebar;
